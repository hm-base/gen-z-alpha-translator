# Archive — NVIDIA-style pipeline (Huimin)

**Status:** archived, not the Friday submission spine.

These documents describe an experimental Days 3–4 upgrade path (Eval → Constraint-First SDG → SFT → optional DPO) using a teacher API (NVIDIA / later Groq). They remain here for credit, methodology Q&A, and future work.

**Still live in the repo (not archived):**
- `src/sdg/`, `src/teacher.py`, `src/eval_icl.py` — code from that track
- `data/raw/synthetic_slang.csv` — still folded into training via `SOURCES` in `src/config.py`

**Submission / demo path:** see the root `README.md` and `docs/superpowers/specs/2026-07-13-genz-slang-translator-design.md`.
